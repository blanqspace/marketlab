[pytest]
markers =
    ibkr: requires live IBKR or simulator
    network: performs outbound network calls
    local: executes environment-dependent checks (tmux/TUI)
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
