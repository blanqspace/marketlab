[pytest]
markers =
    ibkr: requires live IBKR or simulator
    network: performs outbound network calls
    local: executes environment-dependent checks (tmux/session)
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
